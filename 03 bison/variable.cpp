#include <cstdio>
#include <cstdint>
#include <vector>
#include <string>
#include <map>

typedef int64_t i64;
typedef i64 (*char_stream)(void);

struct context {
  std::hashmap<std::string

  void register_variable(const& std::string name, i64 value) {
    names.push_back(name);

  }
};

int read_variable(char_stream read_char) {
  i64 read;
  while ((read = read_char()) != EOF) {
    
  }
}

int main() {
  return 0;
}
